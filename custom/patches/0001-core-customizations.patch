diff --git a/apps/docker/Dockerfile b/apps/docker/Dockerfile
index 0ad7080bf..447f26a4a 100644
--- a/apps/docker/Dockerfile
+++ b/apps/docker/Dockerfile
@@ -53,6 +53,7 @@ ARG CSCRIPTS_DEFAULT_LINKAGE="static"
 ARG CWITH_WARNINGS="ON"
 ARG CMAKE_EXTRA_OPTIONS=""
 ARG GIT_DISCOVERY_ACROSS_FILESYSTEM=1
+ARG BUILD_JOBS=4
 
 ARG CCACHE_DIR="/ccache"
 ARG CCACHE_MAXSIZE="1000MB"
@@ -101,7 +102,7 @@ RUN --mount=type=cache,target=/ccache,sharing=locked \
        -DCMAKE_CXX_COMPILER_LAUNCHER="ccache"          \
        -DCMAKE_C_COMPILER_LAUNCHER="ccache"            \
        -DBoost_USE_STATIC_LIBS="ON"                    \
-    && cmake --build . --config "$CTYPE" -j $(($(nproc) + 1)) \
+    && cmake --build . --config "$CTYPE" -j "$BUILD_JOBS" \
     && cmake --install . --config "$CTYPE"
 
 #############################
diff --git a/apps/docker/Dockerfile.dev-server b/apps/docker/Dockerfile.dev-server
index fab54bd35..607b348d3 100644
--- a/apps/docker/Dockerfile.dev-server
+++ b/apps/docker/Dockerfile.dev-server
@@ -33,7 +33,7 @@ RUN apt-get update \
         # Classic install
         git \
         clang lldb lld clang-format clang-tidy \
-        make cmake \
+        make cmake ninja-build \
         gcc g++ \
         libmysqlclient-dev \
         libssl-dev \
diff --git a/apps/installer/includes/functions.sh b/apps/installer/includes/functions.sh
index 7f179e5c1..20c4e7ba2 100644
--- a/apps/installer/includes/functions.sh
+++ b/apps/installer/includes/functions.sh
@@ -156,6 +156,8 @@ function inst_simple_restarter {
 function inst_download_client_data {
     # change the following version when needed
     local VERSION=v19
+    local DATA_URL="https://github.com/wowgaming/client-data/releases/download/v19/Data.zip"
+    local DATA_SHA256="d37f19cbf3d1c57d965882340519e0275e0964554476117791ad06069a667b04"
 
     echo "#######################"
     echo "Client data downloader"
@@ -178,10 +180,10 @@ function inst_download_client_data {
     fi
 
     echo "Downloading client data in: $zipPath ..."
-    curl -L https://github.com/wowgaming/client-data/releases/download/$VERSION/data.zip > "$zipPath" \
+    curl --fail --location --output "$zipPath" "$DATA_URL" \
+        && printf '%s  %s\n' "$DATA_SHA256" "$zipPath" | sha256sum --check --status \
         && echo "unzip downloaded file in $path..." && unzip -q -o "$zipPath" -d "$path/" \
         && echo "Remove downloaded file" && rm "$zipPath" \
         && echo "INSTALLED_VERSION=$VERSION" > "$dataVersionFile"
 }
 
-
